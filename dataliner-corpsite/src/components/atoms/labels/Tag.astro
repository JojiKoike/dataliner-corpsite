---
type Props = {
  id: string;
  tag: string;
  className?: string;
};

const { id, tag, className = '' } = Astro.props;
---

<span
  id={id}
  class={`rounded-full bg-gray-100 px-3 py-1 text-sm text-gray-700 & ${className}`}
>
  <slot>{tag}</slot>
</span>
