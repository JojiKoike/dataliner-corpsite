---
type Props = {
  tag: string;
  className?: string;
};

const { tag, className = '' } = Astro.props as Props;
---

<span
  class={`rounded-full bg-gray-100 px-3 py-1 text-sm text-gray-700 & ${className}`}
>
  <slot>{tag}</slot>
</span>
